#include<stdio.h>

int main()
{
    int Arr[5] = {10,20,30,40,50};

    printf("Base of address of array:%p\n",Arr);
    printf("size of array is:%p\n",Arr);
    printf("size of array is :%d\n",sizeof(Arr));
    return 0;
}